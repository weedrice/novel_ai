# Gradle JVM configuration
# Enable auto-detection and auto-download of JDKs
org.gradle.java.installations.auto-detect=true
org.gradle.java.installations.auto-download=true

# Configure a toolchain resolver to automatically download JDK
org.gradle.toolchains.foojay-resolver-convention.enabled=true

# Gradle daemon JVM configuration
# NOTE: This path is machine-specific and may need to be updated on other machines
# For other environments, set JAVA_HOME environment variable or update this path
# See README_BUILD.md for more information
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.8.9-hotspot
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8

# Encoding settings
kotlin.code.style=official