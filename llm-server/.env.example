# ==================================================
# Application Configuration
# ==================================================

# Environment: development, staging, production
ENVIRONMENT=development

# Debug Mode (true/false)
DEBUG=false

# Logging Level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ==================================================
# Server Configuration
# ==================================================

# Server Host & Port
HOST=0.0.0.0
PORT=8000

# CORS Configuration
# 개발 환경: http://localhost:3000,http://localhost:3001,http://localhost:8080
# 프로덕션: https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8080

# ==================================================
# Security
# ==================================================

# API Authentication (optional)
# 빈 값으로 두면 인증 비활성화
API_KEY=

# ==================================================
# LLM Provider Configuration
# ==================================================

# LLM Provider Selection
# Options: openai, claude, gemini
DEFAULT_LLM_PROVIDER=openai

# OpenAI (GPT) Configuration
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_TEMPERATURE=0.8
OPENAI_MAX_TOKENS=150
OPENAI_TIMEOUT=30

# Anthropic (Claude) Configuration
ANTHROPIC_API_KEY=your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-3-haiku-20240307
ANTHROPIC_TEMPERATURE=0.8
ANTHROPIC_MAX_TOKENS=150

# Google (Gemini) Configuration
GOOGLE_API_KEY=your-google-api-key-here
GEMINI_MODEL=gemini-pro
GEMINI_TEMPERATURE=0.8
GEMINI_MAX_TOKENS=150

# ==================================================
# Redis Cache Configuration
# ==================================================

# Redis Connection
# 로컬 개발: localhost
# Docker Compose: redis
REDIS_HOST=localhost
REDIS_PORT=6379
# 또는 전체 URL 사용 (위 설정 무시됨)
# REDIS_URL=redis://localhost:6379

# Cache Settings
CACHE_ENABLED=true
CACHE_TTL=3600  # Time-To-Live in seconds (1 hour)

# ==================================================
# Monitoring & Metrics
# ==================================================

# Prometheus Metrics
METRICS_ENABLED=true

# ==================================================
# Development Only
# ==================================================

# Test Mode (자동 설정됨, 수동 설정 불필요)
# TESTING=true
